#include<bits/stdc++.h>
using namespace std;
#define op() ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
#define endl "\n"
#define ll long long int
int main(){
    op();
    string s, f;
    while (cin>>s>>f){
        map<string, int> x;
        for (int i = 0; i < ; i++)
        {
            /* code */
        }
        
    }
    
    return 0;
}