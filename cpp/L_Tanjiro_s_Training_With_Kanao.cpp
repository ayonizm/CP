#include<bits/stdc++.h>
using namespace std;
#define op() ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
#define endl "\n"
int main(){
    op();
    long long int a, b;
    cin >> a >> b;
    vector<int> s1;
    vector<int> s2;
    for (int i = 0; i < a; i++)
    {
        int temp;
        cin >> temp;
        s1.push_back(temp);
    }
    for (int i = 0; i < b; i++)
    {
        int temp;
        cin >> temp;
        s2.push_back(temp);
    }
    for (int i = 0; i < a; i++)
    {
        for (int j = 0; j < b; j++)
        {
            
        }
        
    }
    
    return 0;
}