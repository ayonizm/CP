#include<stdio.h>
#include<string.h>
int main()
{
#ifndef ONLINE_JUDGE
    freopen("input.txt", "r", stdin);
    freopen("output.txt", "w", stdout);
#endif
    int n = 1e5 + 10;
    for (int i = 0; i < n; i++)
    {
        for (int i = 0; i < n; i++)
        {
            if()
        }
        
    }
    
    
    return 0;
}