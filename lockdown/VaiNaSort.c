#include <stdio.h>
int main()
{
    int n;
    while (scanf("%d", &n) != EOF)
    {
        int ar[n];
        if (n == 0)
        {
            break;
        }
        int count = 0;
        for (int i = 0; i < n; i++)
        {
            scanf("%d", &ar[i]);
        }
            int count1 = 0;
            for (int i = 0; i < count; i++)
            {
                /* code */
            }
            
        
    }

    return 0;
}