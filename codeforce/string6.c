#include <stdio.h>
#include<string.h>
int main()
{
    char name[100];
    scanf("%s", name);
    int count = 0;
    // for (int i = 0; name[i] !='\0'; i++)
    // {
        
        
    //     count++;
    // }
    int x = strlen(name);
    printf("%d",x);
    

    return 0;
}