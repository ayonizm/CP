#include <bits/stdc++.h>
using namespace std;
#define op()                      \
    ios_base::sync_with_stdio(0); \
    cin.tie(0);                   \
    cout.tie(0);
#define endl "\n"
#define ll long long int

ll digitsum(ll x)
{
    string k = to_string(x);
    ll sum = 0;
    for (int i = 0; i < k.length(); i++)
    {
        sum += k[i] - '0';
    }
    return sum;
}
map<int, int> x;
void precom()
{
    for (int i = 0; i < ; i++)
    {
        /* code */
    }
    
}
void solve()
{
}
int main()
{
    op();
    solve();
    return 0;
}